{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for notes app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd notes-app/ && npm install && npm run build",
    "start": "node server.js"
  },
  "author": "Michael Alv",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-favicon": "^2.0.1",
    "mongoose": "^5.12.3",
    "path": "^0.12.7"
  }
}
